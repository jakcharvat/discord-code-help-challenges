{
  "name": "~TODO~",
  "version": "0.0.1",
  "scripts": {
    "dev": "make build && svelte-kit dev",
    "build": "make build && svelte-kit build",
    "buildStatic": "svelte-kit build",
    "preview": "make build && svelte-kit preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^1.0.0-next.13",
    "@sveltejs/kit": "next",
    "svelte": "^3.34.0",
    "svelte-check": "^2.0.0",
    "svelte-preprocess": "^4.0.0",
    "tslib": "^2.0.0",
    "typescript": "^4.0.0"
  },
  "type": "module"
}
